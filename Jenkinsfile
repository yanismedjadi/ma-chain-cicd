pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout de la branche 'main'
                echo "recuperer le code"
            }
        }
        stage('Build/Compile') {
            steps {
                // Checkout de la branche 'main'
                echo "Build/Compile"
            }
        }

        stage('Test code') {
            steps {
                // Checkout de la branche 'main'
                echo "Test code"
            }
        }
        stage('Deploiement sur serveur') {
            steps {
                // Checkout de la branche 'main'
                echo "Deploiement sur serveur"
            }
        }

    }
}
